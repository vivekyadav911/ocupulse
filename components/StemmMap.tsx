/**
 * TypeScript default entry for `./StemmMap` imports.
 * Metro still prefers `StemmMap.native.tsx` / `StemmMap.web.tsx` when bundling.
 */
export { StemmMap } from './StemmMap.native';
export type { StemmMapMarker, StemmMapProps } from './StemmMap.types';
